import addresses from "../external/constants";

const approveUniswapSwapRouter = () => {
  const tx = {
    to: addresses.mainnet.uniswap.v3.SwapRouter02,
    value: 0,
    gasLimit: 0,
    chainId: 1
  }
}

const executeUniswapSwapExactInputSingle = () => {
  const tx = {
    to: addresses.mainnet.uniswap.v3.SwapRouter02,
    value: 0,
    gasLimit: 0,
    chainId: 1
  }
}